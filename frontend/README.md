# OctoFit Tracker — Frontend (React)

This folder will contain the React frontend for OctoFit Tracker.

Setup (local):

- `cd frontend` then create the React app (e.g. `npm init react-app .`) when ready.
